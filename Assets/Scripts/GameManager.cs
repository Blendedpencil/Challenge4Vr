using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Vector3 dragonStartCoordinates = new Vector3(26.7f, 1.5f, -3.6f);
        startGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void startGame()
    {
        //opening
        //dragon coming up
        //rocks coming down and dragon landing
        //combat starts
    }

    void RocksFalling()
    {
        //spawn rocks
        //move them down
    }
}
